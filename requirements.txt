# Solstis Voice Assistant Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
asyncio-mqtt>=0.11.0
websockets>=11.0.0
python-dotenv>=1.0.0

# Picovoice for wake word detection
pvporcupine>=3.0.0

# Audio processing
pyaudio>=0.2.11
wave>=0.0.2

# Optional: For better audio processing (if needed)
# numpy>=1.24.0
# scipy>=1.10.0

# System dependencies (install via package manager):
# - ALSA development libraries: sudo apt-get install libasound2-dev (Ubuntu/Debian)
# - PortAudio development libraries: sudo apt-get install portaudio19-dev (Ubuntu/Debian)
# - Python development headers: sudo apt-get install python3-dev (Ubuntu/Debian)
