# Solstis Voice Assistant Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
python-dotenv>=1.0.0

# OpenAI API for GPT-4o mini and TTS
openai>=1.0.0

# ElevenLabs API for TTS and STT
requests>=2.31.0

# Picovoice for wake word detection
pvporcupine>=3.0.0

# GPIO control for reed switch
RPi.GPIO>=0.7.1

# LED strip control for WS281x/NeoPixel LEDs
rpi_ws281x>=4.3.0

# Enhanced semantic analysis dependencies
numpy>=1.24.0
scikit-learn>=1.3.0

# Audio processing (optional - we use ALSA directly)
# pyaudio>=0.2.11

# System dependencies (install via package manager):
# - ALSA development libraries: sudo apt-get install libasound2-dev (Ubuntu/Debian)
# - Python development headers: sudo apt-get install python3-dev (Ubuntu/Debian)
# - Audio utilities: sudo apt-get install alsa-utils mpg123 (Ubuntu/Debian)
#   - alsa-utils: provides aplay/arecord for audio I/O
#   - mpg123: MP3 decoder for ElevenLabs TTS playback
